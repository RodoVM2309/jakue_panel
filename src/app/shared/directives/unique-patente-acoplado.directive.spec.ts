import { UniquePatenteAcopladoDirective } from './unique-patente-acoplado.directive';

describe('UniquePatenteAcopladoDirective', () => {
  it('should create an instance', () => {
    const directive = new UniquePatenteAcopladoDirective();
    expect(directive).toBeTruthy();
  });
});
