/* tslint:disable:no-unused-variable */

import { rucValidatorDirective } from './rucValidator.directive';

describe('Directive: RucValidator', () => {
  it('should create an instance', () => {
    const directive = new rucValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
