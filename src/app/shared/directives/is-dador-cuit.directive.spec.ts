import { IsDadorCuitDirective } from './is-dador-cuit.directive';

describe('IsDadorCuitDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDadorCuitDirective();
    expect(directive).toBeTruthy();
  });
});
